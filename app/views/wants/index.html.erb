<h1>やりたいこと一覧</h1>

<% if @wants.any? %>
  <ul>
    <% @wants.each do |want| %>
      <li>
        <%= want.title %>
        <%= link_to "編集", edit_want_path(want) %></li>
    <% end %>
  </ul>
<% else %>
  <p>まだ登録がありません。</p>
<% end %>
